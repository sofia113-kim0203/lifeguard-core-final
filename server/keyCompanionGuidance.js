/**
 * KEY companion guidance — seat-probe shared constants.
 * Full guidance compose was retired; filler detector preserved for probes.
 */
export const KEY_GENERIC_FILLER_RE = /확인된\s*범위\s*안에서만\s*조심스럽게/;
