# Copy to .env.local for local dev (never commit real keys).
# Design-only skeleton — Supabase project not created by this repo alone.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side feature gates and scheduled-job authorization (names only; never commit values).
KEY_UPLOAD_ENTRY=
COVERAGE_SHEET_LIVE_GATE=
CRON_SECRET=
KEY_CLAUDE_FIRST_DIRECT=
KEY_BORROWED_SENSES=
